import React from 'react'
import { shallow } from 'enzyme'

import IconLocation from './index'

describe('Test IconLocation', () => {
  it('renders without crashing', () => {
    shallow(<IconLocation />)
  })
})
